module rbq_msgs {
  module msg {
    module dds_ {

      @extensibility(APPENDABLE)
      struct ContactInfo_ {
        // Contact wrench (N, N·m)
        float force[3];
        float torque[3];

        // True if contact is detected
        boolean active;
      };

    };
  };
};

